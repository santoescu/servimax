import { Sanitizer } from '@angular/core';
import { HttpClient } from '@angular/common/http';
export declare class MarkdownLoaderService {
    private _http;
    private _sanitizer;
    constructor(_http: HttpClient, _sanitizer: Sanitizer);
    load(url: string, httpOptions?: object): Promise<string>;
}
